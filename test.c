#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>

int main (int argc,char * argv [])
{
	int x=3;
	//kaki;
	sleep(6); //checks timeout error
	//printf("Hello World\n This is a little check file\n change the option you want to check\n in the code itself-");
	//printf(" by comment and uncomment\n");
	//printf("the numbers you inserted are:%s,%s \n",argv[1],argv[2]);	
	//printf("%d\n",x/0); //exception check
	//lafa!!; //compile check
	//return 18; //just random check (score:0, bad output check)
	return 14; //score 100 check
	//return 16; //score 80 check
	//return 84; //another random check
	//return 0;
}
